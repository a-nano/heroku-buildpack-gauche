#!/bin/sh

BUILD_DIR=$1
GAUCHE_DIR=/app/.gauche.d/gauche-0.9.4

cat << EOF
---
config_vars:
  PATH: $PATH:$GAUCHE_DIR/bin
  LD_LIBRARY_PATH: $GAUCHE_DIR/lib
  GAUCHE_LOAD_PATH: $GAUCHE_DIR/share/gauche-0.9/0.9.4/lib
default_process_types:
  console: $GAUCHE_DIR/bin/gosh
EOF
